#include<iostream>
#include<string>
using namespace std;
class generalstore
{
private:
	int iteamcode;
	string iteamname;
	int price;
	int exp_date;
	int exp_month;
	int exp_year;
	int current_date ;
	int current_month;
	int current_year;
public:
	void set();
	void get();
};
void generalstore::set()
{
	cout << "enter the iteam code : ";
	cin >> iteamcode;
	cout << "Enter the iteam name : ";
	cin >> iteamname;
	cout << "enter the price of iteam : ";
	cin >> price;
	cout << "enter the expriation date of iteam : ";
	cin >> exp_date;
	cout << "enter the expriation month of iteam : ";
	cin >> exp_month;
	cout << "enter the expriation year of iteam : ";
	cin >> exp_year;
	cout << "enter the current date of iteam : ";
	cin >> current_date;
	cout << "enter the current month of iteam : ";
	cin >> current_month;
	cout << "enter the current year of iteam : ";
	cin >> current_year;


}
void generalstore::get()
{
	int d;

	if(exp_date<current_date && exp_month<=current_month && exp_year<=current_year){
	cout << "Expired Days: " << exp_date - current_date << endl;
    	if (exp_date >= current_date)
    	{
    		d = price - (price * 40 / 100);
    		cout << "After discount price : " << d << endl;
    	}
	}
	
	if(current_date<exp_date && current_month<=exp_month && current_year<=exp_year){
	cout << "Days Remaining in Expiry: " << exp_date - current_date << endl;
	cout << "Item Price :" << price;}
	
	if(current_date<exp_date && current_month<=exp_month && current_year>=exp_year){
	cout << "Expired Days: " << exp_date - current_date << endl;
    	if (exp_date >= current_date)
    	{
    		d = price - (price * 40 / 100);
    		cout << "After discount price : " << d << endl;
    	}
	}
}
int main()
{
	generalstore myobj1;
	myobj1.set();
	myobj1.get();

	return 0;

}